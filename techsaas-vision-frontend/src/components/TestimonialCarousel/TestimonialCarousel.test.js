import React from 'react';
import { render } from '@testing-library/react';
import TestimonialCarousel from './TestimonialCarousel';
test('renders TestimonialCarousel', () => {
  render(<TestimonialCarousel />);
});
